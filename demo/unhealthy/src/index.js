// This app imports none of its declared dependencies. `venom unused` flags all
// three as dead weight that should be removed.
console.log('An app that never uses any of the packages it depends on.');
