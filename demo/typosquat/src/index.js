// The planted dependency name "expres" is one edit away from the hugely popular
// "express" — a classic typosquat. Venom flags it before it's ever installed.
const app = require('expres');

console.log(app);
